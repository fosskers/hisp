; Find the sum of the even Fib numbers below 4 million.

(require "../prelude.hisp")
(require "../fib.hisp")

(define euler2 () (sum (filter (lambda (n) (even? (n))) (fast-fibs 4000000))))

;(euler2)
